# OSCE Chat Simulator – fresh rebuild

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env      # add your key
streamlit run app/streamlit_app.py
``` 